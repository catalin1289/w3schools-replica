doctype html
html
  //-   baseUrl = daca asta e adevarat    ? true: valoarea de mai jos                         : false valoarea de mai  jos
  - var baseUrl = (taskTarget === 'build' ? 'https://catalin1289.github.io/w3schools-replica' : 'http://localhost:3000')
  block cool-config
  head
    title=myTitle
    meta(name="description", content=myDescription)
    link(rel="stylesheet", href=`${baseUrl}/style.css`)
  body
    include ../header/header
    article
      block awesome-content
    include ../nav/nav
    include ../footer/footer
